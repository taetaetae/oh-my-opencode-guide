# CLAUDE.md - Oh-My-OpenCode 가이드 업데이트 지침

> 이 문서는 AI 에이전트가 oh-my-opencode 버전 업데이트 시 가이드 문서를 자동으로 업데이트하기 위한 지침입니다.

---

## 🚀 빠른 업데이트 (AI용 원클릭 명령)

**사용자가 "버전업 되었으니 문서 업데이트 해줘"라고 하면:**

```bash
# 1단계: 변경사항 분석 스크립트 실행
./scripts/update-docs.sh

# 2단계: 출력된 정보를 바탕으로 문서 업데이트

# 3단계: 빌드 및 배포
bun install && bunx vitepress build docs
git add -A
git commit -m "docs: oh-my-opencode v{새버전} 문서 업데이트"
git push
```

### AI 자동 업데이트 체크리스트

1. **`./scripts/update-docs.sh` 실행** → 변경사항 파악
2. **영역별 문서 업데이트** (아래 매핑 테이블 참조)
3. **VERSION.json 갱신** (새 버전, 날짜, 커밋, changelog)
4. **config.ts 버전 업데이트** (nav의 버전 표시)
5. **빌드 테스트** (`bunx vitepress build docs`)
6. **커밋 & 푸시** → 자동 배포

---

## 📋 프로젝트 개요

이 프로젝트는 [oh-my-opencode](https://github.com/code-yeongyu/oh-my-opencode) 플러그인의 **한국어 사용자 가이드 문서 사이트**입니다.

- **문서 사이트**: https://taetaetae.github.io/oh-my-opencode-guide/
- **GitHub**: https://github.com/taetaetae/oh-my-opencode-guide
- **문서 프레임워크**: VitePress
- **배포**: GitHub Pages (자동)
- **언어**: 한국어
- **대상 독자**: oh-my-opencode를 처음 사용하는 개발자

---

## 🎯 문서의 목적

이 문서는 **oh-my-opencode만의 특징과 활용법**을 알기 쉽게 설명합니다:

1. **두 가지 작업 모드** - Ultrawork(빠른) vs Prometheus(정밀)
2. **전문 에이전트 팀** - 11개 에이전트의 역할과 협력
3. **자동 컨텍스트 주입** - AGENTS.md, Rules 자동 적용
4. **커스텀 워크플로우** - 슬래시 명령어로 반복 작업 자동화
5. **MCP 통합** - 외부 서비스 연동으로 능력 확장

**핵심 방향**: "기본 사용법"보다 **"어떻게 활용하면 효과적인가"**에 집중

---

## 🔄 버전 업데이트 상세 워크플로우

### 1. 현재 버전 확인

```bash
cat VERSION.json
```

### 2. 변경사항 자동 분석

```bash
./scripts/update-docs.sh
```

이 스크립트가 자동으로:
- 현재 문서화된 버전과 최신 버전 비교
- 영역별 변경 파일 목록 출력
- 업데이트가 필요한 문서 안내

### 3. 영역별 문서 매핑

| oh-my-opencode 소스 | 업데이트할 문서 |
|---------------------|-----------------|
| `src/agents/` | `docs/guide/agents.md`, `docs/reference/agents.md` |
| `src/features/builtin-skills/` | `docs/guide/skills.md` |
| `src/hooks/` | `docs/guide/hooks.md`, `docs/reference/hooks.md` |
| `src/features/builtin-commands/` | `docs/guide/commands.md` |
| `src/mcp/` | `docs/guide/mcp.md` |
| `src/config/schema.ts` | `docs/guide/configuration.md`, `docs/reference/config-schema.md` |
| `src/tools/` | `docs/reference/tools.md` |
| `package.json` (version) | `VERSION.json`, `docs/.vitepress/config.ts` |

### 4. 문서 업데이트 수행

변경사항에 따라 해당 문서를 업데이트합니다.

**새 기능 추가 시:**
- 해당 가이드 문서에 설명 추가
- 레퍼런스 문서에 스펙 추가
- 필요시 예제 문서에 활용 예시 추가

**기존 기능 변경 시:**
- 변경된 내용 반영
- Breaking changes는 명확히 표시

### 5. VERSION.json 갱신

```json
{
  "documentedVersion": "새버전",
  "sourceRepo": "https://github.com/code-yeongyu/oh-my-opencode",
  "lastUpdated": "YYYY-MM-DD",
  "lastCommit": "커밋해시",
  "changelog": [
    "이전 변경사항들...",
    "새 변경사항 1",
    "새 변경사항 2"
  ]
}
```

### 6. config.ts 버전 업데이트

```typescript
// docs/.vitepress/config.ts
nav: [
  // ...
  {
    text: 'v새버전',  // ← 여기 업데이트
    items: [
      { text: 'GitHub', link: '...' }
    ]
  }
]
```

### 7. 빌드 테스트

```bash
bun install
bunx vitepress build docs
```

### 8. 커밋 및 푸시

```bash
git add -A
git commit -m "docs: oh-my-opencode v{버전} 문서 업데이트

- 변경사항 1
- 변경사항 2"
git push
# → GitHub Actions가 자동으로 배포
```

---

## 📝 문서 작성 규칙

### 핵심 원칙

1. **활용법에 집중** - "이게 뭐야"보다 **"언제/어떻게 쓰면 효과적인가"**
2. **실용적인 예제** - 복사해서 바로 쓸 수 있는 코드
3. **간결하게** - 불필요한 설명 제거, 핵심만

### 스타일 가이드

- **언어**: 한국어 (기술 용어는 영어 병기 가능)
- **어조**: 친근하고 설명적인 톤
- **구조**: 
  - 각 섹션은 이모지로 시작
  - 코드 예제 필수
  - 비유를 활용한 설명
  - 표와 비교 형식 활용

### VitePress 컴포넌트

```markdown
::: tip 팁
유용한 정보
:::

::: warning 주의
주의사항
:::

::: danger 피해야 할 패턴
반드시 피해야 할 것
:::

::: code-group
```bash [Ultrawork]
ulw 기능 추가해줘
```

```bash [Prometheus]
# Tab 키로 진입
```
:::
```

### 페이지별 작성 방향

| 페이지 | 핵심 메시지 |
|--------|-------------|
| 홈페이지 | oh-my-opencode가 특별한 이유 |
| 소개 | 일반 AI vs 전문가 팀 비교 |
| 빠른 시작 | 두 가지 작업 모드 선택 기준 |
| 에이전트 | 각 에이전트를 **언제** 사용하나 |
| 스킬 | 에이전트에게 전문 지식 부여 |
| 명령어 | 반복 작업 자동화 |
| 훅 | 자동으로 처리되는 것들 |
| Rules | 프로젝트별 규칙 자동 적용 |
| MCP | 외부 서비스로 능력 확장 |
| **베스트 프랙티스** | 10배 효과적으로 사용하는 법 |
| **팁과 트릭** | 숨겨진 기능, 단축키 |
| **문제 해결** | 자주 발생하는 문제와 해결법 |

---

## 🏗️ 프로젝트 구조

```
oh-my-opencode-guide/
├── docs/
│   ├── .vitepress/
│   │   ├── config.ts          # VitePress 설정 (네비게이션, 사이드바, 버전)
│   │   └── theme/
│   │       ├── index.ts       # 테마 설정
│   │       └── custom.css     # 커스텀 스타일
│   ├── guide/                 # 가이드 문서
│   │   ├── index.md           # 소개
│   │   ├── installation.md    # 설치
│   │   ├── quickstart.md      # 빠른 시작
│   │   ├── agents.md          # 에이전트 시스템
│   │   ├── skills.md          # 스킬 시스템
│   │   ├── commands.md        # 슬래시 명령어
│   │   ├── hooks.md           # 훅 시스템
│   │   ├── rules.md           # Rules 파일
│   │   ├── configuration.md   # 설정 가이드
│   │   ├── mcp.md             # MCP 통합
│   │   ├── best-practices.md  # 베스트 프랙티스 ⭐
│   │   ├── tips-and-tricks.md # 팁과 트릭 ⭐
│   │   └── troubleshooting.md # 문제 해결 ⭐
│   ├── examples/              # 워크플로우 예제
│   │   ├── index.md
│   │   ├── new-project.md
│   │   ├── feature-development.md
│   │   ├── bug-fixing.md
│   │   ├── refactoring.md
│   │   ├── code-review.md
│   │   └── documentation.md
│   ├── reference/             # 레퍼런스
│   │   ├── agents.md
│   │   ├── hooks.md
│   │   ├── tools.md
│   │   └── config-schema.md
│   ├── public/                # 정적 파일
│   │   └── logo.svg
│   └── index.md               # 홈페이지
├── scripts/
│   └── update-docs.sh         # 버전 업데이트 도우미 스크립트 ⭐
├── VERSION.json               # 버전 추적
├── CLAUDE.md                  # 이 파일
├── README.md
├── package.json
└── .github/
    └── workflows/
        └── deploy.yml         # GitHub Pages 자동 배포
```

---

## 🚀 로컬 개발

```bash
# 의존성 설치
bun install

# 개발 서버 실행
bun run docs:dev

# 빌드
bunx vitepress build docs

# 빌드 미리보기
bunx vitepress preview docs
```

---

## ✅ 업데이트 체크리스트

새 버전 업데이트 시:

- [ ] `./scripts/update-docs.sh` 실행하여 변경사항 파악
- [ ] 새로운 에이전트 추가 여부 → `agents.md`, `reference/agents.md`
- [ ] 새로운 스킬 추가 여부 → `skills.md`
- [ ] 새로운 훅 추가 여부 → `hooks.md`, `reference/hooks.md`
- [ ] 새로운 명령어 추가 여부 → `commands.md`
- [ ] 설정 스키마 변경 여부 → `configuration.md`, `reference/config-schema.md`
- [ ] MCP 관련 변경 여부 → `mcp.md`
- [ ] 새로운 도구 추가 여부 → `reference/tools.md`
- [ ] 기존 기능의 동작 변경 여부 → 해당 문서
- [ ] VERSION.json 갱신 (버전, 날짜, 커밋, changelog)
- [ ] config.ts 버전 표시 업데이트
- [ ] 빌드 테스트 (`bunx vitepress build docs`)
- [ ] 커밋 및 푸시 (자동 배포)

---

## 🔗 참고 링크

- [oh-my-opencode GitHub](https://github.com/code-yeongyu/oh-my-opencode)
- [oh-my-opencode Releases](https://github.com/code-yeongyu/oh-my-opencode/releases)
- [문서 사이트](https://taetaetae.github.io/oh-my-opencode-guide/)
- [VitePress 문서](https://vitepress.dev/)
